<% @branches.each do |branch| %>
$("#work_package_branch_id_<%= branch.id %>").remove();
<% end %>
$('#branches_inputs').append('<%= escape_javascript(branches_list(@branches)) %>');
location.reload();