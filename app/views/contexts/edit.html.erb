<% html_title l(:label_administration), "#{l(:label_edit)} 'Grupo de Trabalho:' #{h @context.name}" %>
<%= toolbar title: "Grupo de Trabalho: #{h @context.name}"%>

<%= labelled_tabular_form_for(@context) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= styled_button_tag l(:button_save), class: '-highlight -with-icon icon-yes' %>
<% end %>


