<% html_title l(:label_administration), "Novo Grupo de Trabalho"%>

<%= toolbar title: "#{link_to "Grupo de Trabalho", new_context_path} &raquo; #{"Novo Grupo de Trabalho"}"%>

<%= labelled_tabular_form_for(@context) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= styled_button_tag l(:button_create), class: '-highlight -with-icon icon-yes' %>
<% end %>