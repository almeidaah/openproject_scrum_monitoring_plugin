<% html_title l(:label_administration), "Novo Contexto"%>

<%= toolbar title: "#{link_to "Contextos", new_context_path} &raquo; #{"Novo Contexto"}"%>

<%= labelled_tabular_form_for(@context) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= styled_button_tag l(:button_create), class: '-highlight -with-icon icon-yes' %>
<% end %>